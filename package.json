{
  "name": "dash-ui",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev:ui": "pnpm --filter @krislorem/dash-ui dev",
    "dev:ep": "pnpm --filter examples dev",
    "build:ui": "pnpm --filter @krislorem/dash-ui build"
  }
}
